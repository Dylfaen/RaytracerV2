import java.awt.*;

public class LightSource {
    private Point position;
    private Color lightColor;
    public LightSource(Double x, Double y, Double z, Color lightColor) {
        this.lightColor = lightColor;
    }
}
